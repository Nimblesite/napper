// Post-test teardown script (JavaScript)
// Clean up after a playlist run.
// Pure JS — no imports, no npm, no build.

console.log("[teardown] Cleaning up test artifacts...");
console.log(`[teardown] Timestamp: ${new Date().toISOString()}`);
console.log("[teardown] Done");
