[meta]
name = "Get the seeded post (Python ctx)"
description = "Uses {{postId}} set by seed.py, then validates the response in a Python post hook"

[request]
method = GET
url = {{baseUrl}}/posts/{{postId}}

[assert]
status = 200
body.id = {{postId}}

[script]
post = ./validate.py
